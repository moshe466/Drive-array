package xa;

import java.util.List;
import xa.x;

/* loaded from: classes.dex */
public interface c<A, C> {
    List<A> a(ga.s sVar, ia.c cVar);

    C b(x xVar, ga.n nVar, bb.b0 b0Var);

    List<A> c(x xVar, kotlin.reflect.jvm.internal.impl.protobuf.q qVar, b bVar);

    List<A> d(x xVar, kotlin.reflect.jvm.internal.impl.protobuf.q qVar, b bVar, int i10, ga.u uVar);

    List<A> e(x xVar, ga.n nVar);

    List<A> f(x xVar, ga.g gVar);

    List<A> g(x xVar, kotlin.reflect.jvm.internal.impl.protobuf.q qVar, b bVar);

    List<A> h(x.a aVar);

    List<A> i(x xVar, ga.n nVar);

    List<A> j(ga.q qVar, ia.c cVar);
}
